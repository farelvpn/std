lancau
```
apt update && apt upgrade -y && apt install -y wget screen && wget -q https://raw.githubusercontent.com/Rerechan-Store/std/main/setup.sh && chmod +x setup.sh && screen -S setup ./setup.sh
```

uraaaaaaa
# std
